python安装
python基础语法
python的语言类型：
      python是动态语言，强类型语言；
      静态编译语言：
          实现声明变量类型，类型不能改变
          编译时检查
      动态编译语言：
          不用事先声明变量，随时可以赋值为其他类型
          编译时不知道是什么类型，很难推断
      强类型语言：
          不同类型之间操作，必须先强制类型转换为同一类型。print('a'+1)
      弱类型语言：
          不同类型间可以操作，自动隐式转换，JavaScript中console.log(1+'a')
